export declare class Star {
    private x;
    private y;
    private opacity;
    private canvas;
    private length;
    private factor;
    private increment;
    constructor(x: number, y: number, opacity: number, canvas: HTMLCanvasElement);
    draw(): void;
}
export default Star;
